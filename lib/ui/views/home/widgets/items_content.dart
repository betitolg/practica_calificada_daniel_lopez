import 'package:flutter/material.dart';

class ItemsContent extends StatelessWidget {
  const ItemsContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
